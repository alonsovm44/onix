# ideas to implement

- `onix publish` should check if there are uncommitted changes and abort if so
- `onix publish` only works on main/master
- `onix publish` should check if the current branch is up to date with the remote
- `onix publish` should check if the version in the config.yaml is the same as the latest tag
- `onix publish` should check if the version in the config.yaml is greater than the latest tag
