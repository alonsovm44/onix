Implement a function to detect the current system's OS and Architecture to help the Install command find the right binary artifact.
How do I update the clap definition in main.rs to support the --v flag for the publish command? also, if the author tries to publish an already published version, warn and ask for confirmation y/n. 
